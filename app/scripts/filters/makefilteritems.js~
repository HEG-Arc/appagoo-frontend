'use strict';

/**
 * @ngdoc filter
 * @name appagooApp.filter:makeFilterItems
 * @function
 * @description
 * # makeFilterItems
 * Filter in the appagooApp.
 */
angular.module('appagooApp')
  .filter('makeFilterItems', function () {
    return function (input) {
      return 'makeFilterItems filter: ' + input;
    };
  });
